#include <iostream> // Sem importar libs, nada acontece aqui kkkk

using namespace std; // std significa standard, padrão

// o código fora de função, aqui precisa estar dentro da função main
// por ser uma lingaguem fortemente tipada, tem que sair tipando tudo
int main() {
    cout << "Hello World\nJovem Padawan"; 
    /* c out é saída, 
    << pra atribuir
    precisa da merda do ; no fim das sentenças
    e antes também precisa do << endl pra informar que acabou a linha */ 
    return 0; // default
}

// não existe indentação por tab aqui, mas é delicinha, snake style!